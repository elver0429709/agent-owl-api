services:
  - type: web
    name: agent-owl-api
    env: docker
    dockerfilePath: .container/Dockerfile
    dockerContext: .
    buildCommand: |
      cp -r .container container_temp
      docker build -f container_temp/Dockerfile -t agent-owl .
    startCommand: npm start
