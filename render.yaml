services:
  - type: web
    name: agent-owl-api
    env: docker
    dockerContext: .
    dockerfilePath: .container/Dockerfile
    buildCommand: |
      echo "📦 Listando archivos dentro de .container:"
      ls -R .container || echo "⚠️ No se encontró la carpeta .container"
    startCommand: npm start
