services:
  - type: web
    name: owl-app
    env: python
    plan: free
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt"
   python onl_whispe.py
# o
python online_whisper.py
