services:
  - type: web
    name: agent-owl-api
    env: docker
    dockerContext: .
    dockerFilePath: container/Dockerfile
    buildCommand: |
      echo "🚀 Copiando desde la raíz del repositorio..."
    startCommand: python owl/main.py
