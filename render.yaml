services:
  - type: web
    name: agent-owl-api
    env: docker
    dockerContext: .
    buildCommand: |
      echo "🔧 Copiando carpeta .container para el build..."
      mkdir -p visible_container
      cp -r .container/* visible_container/
      echo "🚀 Construyendo imagen desde visible_container/Dockerfile..."
      docker build -f visible_container/Dockerfile -t agent-owl .
    startCommand: npm start
