services:
  - type: web
    name: owl-app
    env: python
    plan: free
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt"
   test: run python3 --version to confirm interpreter

