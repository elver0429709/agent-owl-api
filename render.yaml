services:
  - type: web
    name: agent-owl-api
    env: docker
    dockerContext: .
    dockerfilePath: .container/Dockerfile
    buildCommand: |
      echo "📦 Listando archivos disponibles:"
      ls -R .container
      echo "🚀 Construyendo imagen Docker..."
      docker build -f .container/Dockerfile -t agent-owl .
    startCommand: npm start
