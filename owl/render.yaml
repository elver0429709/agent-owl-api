services:
  - type: web
    name: agent-owl-api
    env: python
    plan: free
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt"
    startCommand: "python3 owl/webapp.py"
    rootDir: .
